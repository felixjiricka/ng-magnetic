import { NgMagneticDirective } from './ng-magnetic.directive';

describe('NgMagneticDirective', () => {
  it('should create an instance', () => {
    const directive = new NgMagneticDirective();
    expect(directive).toBeTruthy();
  });
});
